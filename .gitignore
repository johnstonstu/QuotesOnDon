# build output
dist/
.astro/

# dependencies
node_modules/

# local env / secrets
.env
.env.*
!.env.example

# ingest / dashboard scratch
data/candidates/
data/ingest-log/
!data/candidates/.gitkeep

# os / editor noise
.DS_Store
*.log
# X inbox working files (input by hand, archived after a run)
data/x-inbox.txt
data/x-inbox.processed.txt
