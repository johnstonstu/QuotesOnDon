{
  "task_id": "task-01-mirror-survey",
  "base": "main",
  "title": "Fallback sources for his first-person posts",
  "question": "If the trumpstruth.org archive feed disappeared tomorrow, which other public sources carry the same posts, and how do their ids map to the canonical truthsocial.com post URL?",
  "why": "That feed is currently the only automated path for first-person posts, so it is a single point of failure for the whole store.",
  "allowed_paths": [
    "research/"
  ],
  "excluded_paths": [
    "src/",
    "tools/",
    "data/quotes/",
    "data/candidates/",
    "data/schema/",
    "data/sources.json",
    "public/",
    "package.json",
    "package-lock.json",
    ".github/",
    "astro.config.mjs",
    "README.md"
  ],
  "deliverable": "research/mirror-survey.md",
  "acceptance_command": "node tools/check-research.mjs research/mirror-survey.md",
  "must_include": [
    "at least three candidates, each probed with a real HTTP request and its status recorded",
    "for each: does it carry the canonical truthsocial.com URL or original id, or only its own id?",
    "stability signals: who runs it, is there robots.txt, does it rate-limit",
    "a ranked recommendation with the trade-off stated, including 'do not use' where the route requires disguising the client"
  ],
  "boundaries": [
    "Read-only outside research/: no edits to the store, the site, the tools or CI, and no commits to main.",
    "Never present a model's wording as a source's wording. If you cannot fetch it, it is not evidence.",
    "No accounts, no purchases, no form submissions, no rate-limit evasion.",
    "Do not fetch truthsocial.com itself with a spoofed browser User-Agent. If a source only works that way, record that as the finding and disqualify it.",
    "Record failures as findings (dead, blocked, paywalled), do not route around them."
  ],
  "engine": "coordinator-choice",
  "model": "coordinator-choice",
  "bounds": {
    "max_runtime_minutes": 30,
    "max_tool_calls": 40,
    "max_spend_usd": 0,
    "network": "read-only HTTP GET; no form submissions, no account creation, no purchases"
  },
  "commit_instruction": "One commit on the worker branch containing only the report file. Do not merge, do not push to main, do not open a PR — the coordinator decides what is adopted.",
  "report_requirements": "See research/README.md — six required sections, every finding backed by a fetched URL."
}
